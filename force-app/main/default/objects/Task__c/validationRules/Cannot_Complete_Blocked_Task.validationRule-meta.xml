<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cannot_Complete_Blocked_Task</fullName>
    <active>true</active>
    <description>Prevents marking task as complete if it has status Blocked</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status__c, "Completed"),
    ISPICKVAL(PRIORVALUE(Status__c), "Blocked"),
    ISCHANGED(Status__c)
)</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>Cannot mark blocked task as complete. Please resolve blocking issue first or change status to In Progress.</errorMessage>
</ValidationRule>
