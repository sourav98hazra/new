<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Checklist_Required_Before_Complete</fullName>
    <active>true</active>
    <description>Ensures all checklist items are completed before marking task as complete</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status__c, "Completed"),
    ISCHANGED(Status__c),
    Checklist_Complete__c = FALSE
)</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>All checklist items must be completed before marking task as complete. Please complete all checklist items first.</errorMessage>
</ValidationRule>
